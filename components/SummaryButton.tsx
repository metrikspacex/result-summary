import styles from "@/styles/summary/SummaryButton.module.scss";

export default function SummaryButton() {
  return (
    <button className={styles.root} type="button">
      Continue
    </button>
  );
}
